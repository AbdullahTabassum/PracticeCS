package Graphs.MinimumSpanningTrees;

import Graphs.Node;
import Graphs.ShortestPaths.Edge;

import java.util.Set;

public class Prims {

    public static Set<Edge> findMinimumSpanningTree(Set<Node> graph) {

        //start at an arbitrary node

        //keep goind until all nodes have been taken out from the graph

        //keep all of the edges in a priority queue

        //pop the lowest cost edge and add its dst node

        // add the new nodes edges into the priority queue and repeat.

        //this is very similar to dijkstras

        return null;
    }


}
