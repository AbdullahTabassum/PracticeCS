package Strings;

public class FindDuplicates {

    public static boolean isDuplicate(String s) {
        //go through each character in string and set the bit vectors bit corresponding to it
        int bitVector = 0;

        for(char c : s.toCharArray()) {

            //bitVector =
        }
        return false;
    }

    public static int setBitVecor(int vector, int val) {

        //set the val'th value of vector to a 1 if it is a zero
        int mask = 1 << val;

        return 0;
    }
}
